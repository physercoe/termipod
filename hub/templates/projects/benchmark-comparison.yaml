name: benchmark-comparison
kind: goal
goal: |-
  Compare {models} on {benchmark} with {samples} samples; rank by {headline_metric} and write a comparison memo naming the winner and the margin.
parameters:
  models: []
  benchmark: ""
  samples: 100
  headline_metric: accuracy
on_create_template_id: agents.steward
