-- Irreversible by design: the stub template YAML is removed from the tree, so
-- there is nothing to re-seed. Down is a no-op (a fresh Init re-seeds only the
-- templates still shipped under templates/projects/).
SELECT 1;
