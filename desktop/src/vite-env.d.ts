/// <reference types="vite/client" />

// Injected by Vite at build time (vite.config.ts `define`) — the app version
// from package.json, available synchronously so the UI paints it on first frame.
declare const __APP_VERSION__: string;
